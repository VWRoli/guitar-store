import Filters from './Filters';
import ProductsList from './ProductsList';

const Products = () => {
  return (
    <div>
      <h2>Products</h2>
      <Filters />
      <ProductsList />
    </div>
  );
};

export default Products;
