const defaultState = {
  cart: [],
  compare: [],
  total: 0,
  amount: 0,
  isMessageOpen: false,
};

const cartReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default cartReducer;
